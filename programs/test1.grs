{
    let b = 'b';
    let c = 'c';
    func x(a) {
        println(c);

        func y() {
            func z(a, b, c) {
                println(c);
                println(`{a}{b}`);
            }

            z(a, b, c);
        }

        return y;
    }

    let a = x('a');
    a();
}